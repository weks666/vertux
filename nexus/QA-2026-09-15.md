# Проверка лендинга Nexus: Invest Workspace

Дата: 15 сентября 2026. Исходная версия сайта: `49add8aa764f2aee69d65de1321eb360282f6784` в `weks666/vertux`, GitHub Pages `main:/`, домен `vertux.online`. Предыдущая версия сохранена в Git; изменение ограничено `nexus/`. Существующее незакоммиченное изменение корневого `script.js` не входит в выпуск.

## Изменение

Два направления на первом экране, отдельный Invest перед бизнес-демонстрацией, скриншот учебной сборки, одинаковый план по срокам 1/3/12 месяцев, статус будущего запуска, FAQ о брокерах, AI, пробном периоде и Windows. Контактная почта отображается адресом. Доработаны типографика, мобильные отступы, контраст новых CTA и закрытие меню по Escape/переходу/расширению экрана.

## Локальные проверки

- JavaScript syntax и `git diff --check` — PASS.
- 312 уникальных ключей главной страницы имеют RU/EN-переводы; проверены метаданные, aria-label и alt нового изображения. Локальные ссылки и ассеты существуют, HTML IDs уникальны.
- Браузер: фактические ширины 320, 390, 768, 1280, 1440 px. Обнаруженное переполнение старой сценой устранено ограничением её декоративных слоёв; повторная проверка без горизонтального переполнения документа.
- RU/EN, сохранение языка после перехода/возврата, якорь Invest ниже header, мобильное меню, Escape и возврат фокуса, нативный FAQ по Enter — PASS.
- Прежние вкладки Process → AI and knowledge клавиатурой и Operations мышью — PASS.
- 43 текстовых узла новых поверхностей: расчёт по computed styles, минимум 5.94:1, нарушений выбранных порогов 4.5:1/3:1 нет. Это проверка изменённой поверхности, не полный аудит всего унаследованного сайта.
- Увеличенный скриншот открывается как изображение 1440 × 1814; Back возвращает на лендинг. Изображение 356105 байт; SHA-256 `beeb06b2f543eb91130816a2c838d4ddf48608a76304f7756be87a31a2bb45de`, совпадает с исходным учебным снимком.
- Консоль браузера: ошибок и предупреждений нет. Локальные главная, download, account, updates и PNG возвращают HTTP 200.
- Reduced-motion: проверена ветка JS с изолированным matchMedia и CSS-отключение движения; отдельная эмуляция этого режима в браузере не выполнялась. Основное содержимое Invest, цены и статус присутствуют в исходном HTML без JS, FAQ использует native details.
- Impeccable detector выполнен один раз. Парсеры недоступны: fallback regex, нулевой результат не использовался как доказательство качества. Выполнены отдельный визуальный проход и проверка computed styles.
- В изменённых текстовых файлах не найдено распространённых форматов API/private keys. Изображение содержит только учебные данные. Download Gate, установщик, платежи и учётные записи не менялись.

## Граница выпуска

Публикуется информационная витрина. Она не включает массовые продажи, реальную оплату, выдачу аккаунтов, AI-квоты, пробный период или интеграции с новыми брокерами. GitHub Pages build и совпадение опубликованных HTML/JS/CSS/PNG с локальными SHA проверяются после push; результат фиксируется в ответе задачи и истории публикации GitHub.
